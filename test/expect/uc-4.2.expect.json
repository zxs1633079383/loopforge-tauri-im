{
  "$schema-note": "四面期望文件 (UC-4.2 按需 sync notify) — oracle 本体。只读契约 (contract-readonly-autofix.md §1 / harness C004)：自动修复 agent 禁改本文件让红变绿；红只能靠改 helix 引擎实现或本仓渲染壳。契约真过时 → 出『契约变更提案 + 证据』交人审。",
  "ucId": "UC-4.2",
  "title": "cursor 落后（gap）→ 引擎自驱 channel/sync/notify → server 回放离线区间事件 → im:channel:update-by-post（badge）+ im:post:received（增量行）→ DOM data-unread badge + 增量消息行 → DB message + channel_event_cursor 跳空洞",
  "contractReadonly": true,
  "contractSources": {
    "outbound": "helix/crates/helix-im/src/acl/sync_http_effects.rs::sync_notify（POST channel/sync/notify·body {cursors:[{channelId, fromSeq}]}·per-channel·全 camelCase）。真源 partials/8 §2.1（helix ledger 实证 needSync=true 真帧）。注：真机curl真源.md 仅录 6 个前端命令端点·gap 触发的 sync/notify wire 不在其中·此处真源 = helix outbound 工厂源码。",
    "projection": "helix/crates/helix-im/src/acl/projection_control_effects.rs::emit_channel_update_by_post（im:channel:update-by-post·{channel_id, event_seq, msg_id}·瘦·channel-row badge）+ sync_effects.rs::sync_mutation_emits（逐 PostUpsert emit update-by-post + 配对 fat im:post:received）+ projection-schema.md 行 67。",
    "dom": "docs/spec/angular-ui-plan.md CL 区 data-unread badge（4.2 sync 增量后 badge·行 75）+ ML 区增量消息行 data-msg-id（im:post:received 驱动追加）。",
    "storage": "helix/crates/helix-im/src/acl/sync_effects.rs::batch_upsert_events_with_messages（sync 回放逐 PostUpsert → batch_upsert message）+ advance_cursor（monotonic_upsert channel_event_cursor 跳空洞·旁证）。"
  },

  "corrAnchor": {
    "note": "领域锚 = channelId（ch 维·gap 落后频道）。UC-4.2 内核自驱·gap 触发·非前端 round-trip。① 出站 channel/sync/notify body.cursors[0].channelId = 锚 ch（decorator 探 body.cursors[0] 抽 ch → ① keyed by ch）；② update-by-post + ④ message 各带 channel_id = 锚 ch（散在 per-event 多束·reducer runFourFacetSyncNotify 按窗口 + 锚 ch 各 facet 聚合裁定·非单 corr_key 束）。e2e 运行时把 sync 命中的真实落后频道 id 填入本字段 ch。",
    "ch": null
  },

  "outbound": {
    "method": "POST",
    "urlEndsWith": "channel/sync/notify",
    "bodyFields": {
      "cursors": "*"
    },
    "bodyForbidden": ["channel_id", "from_seq", "channelId", "fromSeq"],
    "_note": "sync_notify：body {cursors:[{channelId, fromSeq}]}（per-channel·cursors 数组·内层元素 camelCase channelId/fromSeq）。顶层只有 cursors 键（无 timestamp·区别于 UC-4.1 batch increment 的 {timestamp, cursors}）。bodyForbidden 锚顶层不该出现 channelId/channel_id/fromSeq/from_seq（它们应嵌在 cursors[] 内·非顶层泄漏）。值用 * = 仅校验键存在·内层 channelId 覆盖锚 ch 由 reducer faithful 命中（cursors[0].channelId === anchorCh）。"
  },

  "projection": {
    "event": "im:channel:update-by-post",
    "dataKeys": ["channel_id", "event_seq", "msg_id"],
    "_note": "projection-schema 行 67 emit_channel_update_by_post：data {channel_id, event_seq, msg_id}（瘦·留 channel-row·sync 回放每条可见 type1 新消息触发·badge 信号）。严格键集（缺/多即 fail）。配对 fat im:post:received（增量消息行驱动·非本面锚）经 MESSAGE_ROW_CHANNELS 分支 applyMessageItem 追加 ML 行。"
  },

  "storage": {
    "op": "batch_upsert",
    "table": "message",
    "minRows": 1,
    "_note": "batch_upsert_events_with_messages → sync 回放逐 PostUpsert event 落 batch_upsert message（每事件 rows=1·reducer 按锚 ch 累计 ≥ minRows）。channel_event_cursor monotonic_upsert（scope=ch·value=high-water·跳空洞）旁证 cursor 推进·payload 无 rows 不作 minRows 锚（④ 锚 message batch_upsert·与 UC-4.1 同纪律）。"
  },

  "dom": {
    "dataAttrs": {
      "unread": "*",
      "msg-id": "*"
    },
    "_note": "issue #32 ③：① data-unread badge 非空（CL 区锚频道行·im:channel:update-by-post 每条 +1·sync 增量后未读累加）；② 增量消息行 data-msg-id 非空（ML 区·fat im:post:received 驱动 applyMessageItem 追加·sync 回放消息上屏）。e2e 读锚频道 CL 行 [data-channel-id=<ch>] 的 data-unread + 该频道 ML 任一 [data-msg-id] 增量行注入 reducer。"
  }
}
