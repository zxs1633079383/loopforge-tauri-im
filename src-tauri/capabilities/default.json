{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "主窗口最小权限：core 默认（event listen/emit + window）。",
  "windows": ["main"],
  "permissions": ["core:default"]
}
