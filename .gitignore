# Rust
/target/
**/*.rs.bk
Cargo.lock

# 前端 / Tauri（后续接入）
/node_modules/
/dist/
/.angular/
/src-tauri/target/
/src-tauri/gen/

# 测试产物 / tape 临时
*.db
*.db-wal
*.db-shm
/test/reports/

# 本地 profile 覆盖（不入仓；config/*.json 入仓，仅此覆盖文件本地态）
/config/active-profile

# OS
.DS_Store
/worktrees/
